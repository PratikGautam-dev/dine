.worktrees/
assets/

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# Env files -- never commit real secrets (see backend/.env.example for the
# template every real .env/.env.production is generated from).
.env
.env.*.local
.env.production

# Local SQLite/dev artifacts and logs
*.db
*.log

# Design-reference material (source mockups + reference-vs-built comparison screenshots) -- local-only working
# files for a design pass, never app assets; kept out of git entirely rather than committed and later pruned.
design-reference/

# Next.js
frontend/.next/
frontend/node_modules/
node_modules/
